import { AqiHighlightDirective } from "./aqi-highlight.directive";


describe('AqiHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AqiHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
