import { LastUpdatedPipe } from './last-updated.pipe';

describe('LastUpdatedPipe', () => {
  it('create an instance', () => {
    const pipe = new LastUpdatedPipe();
    expect(pipe).toBeTruthy();
  });
});
